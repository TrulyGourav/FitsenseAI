spring:

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:fitness_userservice_db}
    username: ${DB_USERNAME:gourav.singh}
    password: ${DB_PASSWORD:admin}

  jpa:
    hibernate:
      ddl-auto: update   # convenient for dev
    show-sql: true

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql: TRACE
